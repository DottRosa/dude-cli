/* eslint-disable no-console */

const logService = {
  success(message) {
    console.log(message);
  },

  error(message) {
    console.error(message);
  },

  info(message) {
    console.log(message);
  },
};

export default logService;
