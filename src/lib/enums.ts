export enum CONFIG_ACTION_FIELD_TYPES_ENUM {
  STRING = 'string',
  LIST = 'list',
}

export enum CONFIG_VALIDATION_LOG_LEVEL_ENUM {
  INFO = 'info',
  WARNING = 'warning',
  ERROR = 'error',
}

export enum CONFIG_VALIDATION_LOG_MESSAGE_TYPES_ENUM {
  MISSING_FIELD = 'missing_field',
  WRONG_VALUE = 'wrong_value',
  EMPTY_LIST = 'empty_list',
}

export enum CONFIG_VALIDATION_LOG_MESSAGE_FIELD_TYPES_ENUM {
  STRING = 'string',
  BOOLEAN = 'boolean',
  LIST = 'list',
}
